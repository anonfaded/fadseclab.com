# Header and Nav
- The user prefers the previous "floating type" navbar design over the redesigned thin bar with `//` sigils. Revert to the prior header treatment. The navbar should feel like a macOS-style floating/docked bar, not a flat solid bar that fills the full width. Confidence: 0.85
- Account button is too plain when it's just a text "Account" label. Give it a more deliberate visual treatment (icon, frame, or branded affordance). Confidence: 0.70
- The account button must not move/translate on hover (no upward lift, no scale shift). It can still change color/background, but its position stays static. Confidence: 0.75
- The nav "Contact" CTA button should not use the brand red fill. Use a more neutral/default button treatment for Contact; reserve the red for the primary hero CTA or similar high-prominence actions, not every nav button. Confidence: 0.70
- Do not add a red circular hover glow/halo to nav buttons. The hover affordance on header buttons should be subtle (background tint or border), not a red circle/radial effect. Confidence: 0.70
- Nav links must scroll correctly to their target sections. Verify anchor IDs match on every nav link, including mobile menu. Confidence: 0.80
- Nav links should use the same `//` mono-sigil labeling pattern as other section headings for consistency. Do not strip `//` from the nav while keeping it on body section heads — pick one labeling system and apply it everywhere. Confidence: 0.70
- The navbar should be dynamic on scroll — shrink down (height, padding, or both) once the user scrolls past the hero. A static-size sticky header reads as inert. The shrink trigger should fire as soon as the user leaves the top of the page — not wait until a deep section like "products" is reached. Confidence: 0.80
- The header's light-mode shadow is too dark and looks bad. Use a soft, low-contrast shadow (or none) in light mode. Heavy dark shadows in a light surface read as unprofessional. Confidence: 0.75
- Do not pair a random shield/glyph SVG with the company name in the brand lockup — it reads as unpolished. Either drop the mark and let the wordmark stand alone in a strong display font, or use a real, deliberate brand mark. Confidence: 0.75
- The dynamic-on-scroll navbar must actually transition visibly between states (transparent hero → bordered/blurred scrolled). An "always transparent" header reads as broken even if the JS state is correct. Verify the visual difference between states is real and pleasant. Confidence: 0.85
- Do not use an italic "Lab" suffix or a mid-word dot separator in the brand wordmark. They read as fussy/decoration. Use a space (e.g. "FadSec Lab") to separate the words — not a dot, not a tight join. Keep the wordmark typographically clean — strong display face for the full name, with "Lab" distinguished only by color (red). Confidence: 0.80
- Reuse the footer's flag/name swap animation in the header brand lockup: show the wordmark by default, on hover swap to the FadSec Lab flag, and swap back to the name on hover-leave. Do not invent a different interaction. Do not stack a separate eyebrow line (e.g. "FSL · PRIVACY-FIRST") above the wordmark — the footer already has the flag/wordmark pattern, mirror it. (Superseded — user reversed this: "its bad lets remove it. and revert, keep it simple as it is.") Confidence: 0.40
- When the user gives a narrow fix instruction ("just fix its size"), do exactly that — resize the element — and keep the existing animation/structure intact. Do not redesign the container, add borders, change overflow, or alter the surrounding lockup while "fixing" a size. The flag swap animation should keep the same motion; only its dimensions change. Confidence: 0.90
- Do not add a flag-swap hover animation to the header brand lockup. The user rejected it ("its bad lets remove it. and revert, keep it simple as it is.") — the header should just show the wordmark statically, no hover swap, no flag. Keep it simple. Confidence: 0.85
- When reverting/simplifying an element per the user's instruction, strip ALL related hover/transition effects (CSS transitions, transforms, `:hover` rules, `will-change`, overflow containers used for the animation) — not just the primary feature. Leaving residual hover CSS on the wordmark after removing the flag still violates "keep it simple" and triggers another correction. Confidence: 0.90
- The brand wordmark should be styled as ALL CAPS with a **red**-colored `//` sigil prefix (e.g. `// FADSEC LAB`), matching the mono-label treatment used elsewhere on the site. The user prefers this label-like framing over plain mixed-case text. Keep "Lab" in brand red for the color distinction. The `//` sigil on the wordmark is RED; nav-link `//` sigils are gray (subtle). Don't mix them. Confidence: 0.85
- When the user gives a styling instruction targeting a specific element (e.g. "the company name", "the brand wordmark"), apply it ONLY to that element. Do not propagate the same treatment to nearby elements (nav links, mobile menu, etc.) as a consistency pass. The user has corrected this scope drift multiple times — generalize only when explicitly asked. Confidence: 0.95
- The nav-link `//` sigils (gray by default) should turn red on hover. This is a separate affordance from the brand wordmark's red `//` — brand sigil is red at rest, nav sigils are gray at rest and go red on hover. Confidence: 0.80
